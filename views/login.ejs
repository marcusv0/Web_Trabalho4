<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Portal de Acesso</title>
    <style>
        /* Mantive as variáveis CSS originais para preservar o design */
        :root {
            --primary-color: #007bff;
            --success-color: #28a745;
            --danger-color: #dc3545;
            --text-color: #343a40;
            --bg-color: #e9ecef;
            --card-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex; 
            justify-content: center; 
            align-items: center; 
            height: 100vh; 
            margin: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
        }

        .login-box {
            background: white;
            padding: 40px; 
            border-radius: 12px; 
            box-shadow: var(--card-shadow); 
            width: 350px;
            transition: transform 0.3s ease;
        }

        .login-box:hover {
            transform: translateY(-5px);
        }

        h2 {
            text-align: center;
            color: var(--primary-color);
            margin-bottom: 25px;
            font-weight: 600;
        }

        label {
            font-weight: 500;
            display: block;
            margin-bottom: 5px;
        }

        input[type="email"], input[type="password"] { 
            width: 100%;
            padding: 12px; 
            margin-bottom: 20px;
            border: 1px solid #ced4da;
            border-radius: 6px; 
            box-sizing: border-box; 
            transition: border-color 0.3s;
        }

        input[type="email"]:focus, input[type="password"]:focus {
            border-color: var(--primary-color);
            outline: none;
            box-shadow: 0 0 0 0.1rem rgba(0, 123, 255, 0.25);
        }

        button { 
            background-color: var(--primary-color);
            color: white; 
            padding: 15px 20px; 
            margin-top: 10px;
            border: none; 
            cursor: pointer; 
            width: 100%; 
            border-radius: 6px; 
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }

        button:hover {
            background-color: #0056b3;
            box-shadow: 0 0.2rem 0.5rem rgba(0, 0, 0, 0.2);
        }

        .message-error {
            color: var(--danger-color);
            background-color: #f8d7da;
            border: 1px solid var(--danger-color);
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 15px;
            text-align: center;
        }
        
        .message-success {
            color: var(--success-color);
            background-color: #d4edda;
            border: 1px solid var(--success-color);
            padding: 10px;
            border-radius: 4px;
            margin-bottom: 15px;
            text-align: center;
        }

        .link-cadastro {
            margin-top: 20px;
            text-align: center;
            font-size: 14px;
        }
        
        .link-cadastro a {
            color: var(--primary-color);
            text-decoration: none;
        }
        
        .link-cadastro a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <div class="login-box">
        <h2>Login Seguro</h2>

        <% if (typeof erro !== 'undefined' && erro) { %>
            <p class="message-error">
                <% if (erro === 'usuario_nao_encontrado') { %>
                    E-mail não localizado na base.
                <% } else if (erro === 'senha_incorreta') { %>
                    A senha informada é inválida.
                <% } else { %>
                    Realize o login para acessar.
                <% } %>
            </p>
        <% } %>
        
        <% if (typeof sucesso !== 'undefined' && sucesso === 'cadastro') { %>
            <p class="message-success">
                Conta criada! Pode entrar agora.
            </p>
        <% } %>

        <form action="/login" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            
            <label for="email">E-mail:</label>
            <input type="email" placeholder="usuario@dominio.com" name="email" required>

            <label for="senha">Senha:</label>
            <input type="password" placeholder="Digite sua senha" name="senha" required>
            
            <button type="submit">Entrar no Sistema</button>
        </form>

        <p class="link-cadastro"><a href="/register">Ainda não possui conta? 
            Criar agora.</a></p>
    </div>

</body>
</html>